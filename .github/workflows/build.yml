name: build

on:
    push:
        branches:
            - "*"
    pull_request:
        branches:
            - "*"

jobs:
    test:
        if: "!contains(github.event.head_commit.message, 'skip ci')"
        strategy:
            fail-fast: false
            matrix:
                include:
                    - laravel: 13.*
                      testbench: 11.*
                      php: 8.4
                      unitconfig: phpunit_gt_81.xml
                      os: ubuntu-latest
                      dependency-version: prefer-stable
                    - laravel: 13.*
                      testbench: 11.*
                      php: 8.3
                      unitconfig: phpunit_gt_81.xml
                      os: ubuntu-latest
                      dependency-version: prefer-stable
                    - laravel: 12.*
                      testbench: 10.*
                      php: 8.4
                      unitconfig: phpunit_gt_81.xml
                      os: ubuntu-latest
                      dependency-version: prefer-stable
                    - laravel: 11.*
                      testbench: 9.*
                      php: 8.4
                      unitconfig: phpunit_gt_81.xml
                      os: ubuntu-latest
                      dependency-version: prefer-stable
                    - laravel: 12.*
                      testbench: 10.*
                      php: 8.3
                      unitconfig: phpunit_gt_81.xml
                      os: ubuntu-latest
                      dependency-version: prefer-stable
                    - laravel: 11.*
                      testbench: 9.*
                      php: 8.3
                      unitconfig: phpunit_gt_81.xml
                      os: ubuntu-latest
                      dependency-version: prefer-stable
                    - laravel: 12.*
                      testbench: 10.*
                      php: 8.2
                      unitconfig: phpunit_gt_81.xml
                      os: ubuntu-latest
                      dependency-version: prefer-stable
                    - laravel: 11.*
                      testbench: 9.*
                      php: 8.2
                      unitconfig: phpunit_gt_81.xml
                      os: ubuntu-latest
                      dependency-version: prefer-stable
        runs-on: ${{ matrix.os }}
        name: P${{ matrix.php }} - L${{ matrix.laravel }} - ${{ matrix.dependency-version }} - ${{ matrix.os }}
        env:
            extensions: intl, json, mbstring, dom, curl, openssl, gd

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Cache dependencies
              uses: actions/cache@v4
              with:
                  path: ~/.composer/cache/files
                  key: dependencies-laravel-${{ matrix.laravel }}-php-${{ matrix.php }}-composer-${{ hashFiles('composer.json') }}

            - name: Setup PHP extensions
              id: cache-env
              uses: shivammathur/cache-extensions@v1
              with:
                  php-version: ${{ matrix.php }}
                  extensions: ${{ env.extensions }}
                  key: php-extensions-cache-v1

            - name: Setup PHP ${{ matrix.php }}
              uses: shivammathur/setup-php@v2
              with:
                  php-version: ${{ matrix.php }}
                  extensions: ${{ env.extensions }}
                  tools: composer
              env:
                  COMPOSER_TOKEN: ${{ secrets.GITHUB_TOKEN }}

            - name: Setup Laravel ${{ matrix.laravel }}
              run: |
                  composer require "laravel/framework:${{ matrix.laravel }}" "orchestra/testbench:${{ matrix.testbench }}" --no-interaction --no-update
                  composer update --${{ matrix.dependency-version }} --prefer-dist --no-interaction --no-suggest

            - name: Run tests
              run: |
                  vendor/bin/phpunit -c ${{ matrix.unitconfig }} --no-coverage

    coverage:
        if: "!contains(github.event.head_commit.message, 'skip ci')"
        runs-on: ubuntu-latest
        name: Coverage P8.4 - L13
        env:
            extensions: intl, json, mbstring, dom, curl, openssl, gd

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup PHP 8.4
              uses: shivammathur/setup-php@v2
              with:
                  php-version: 8.4
                  extensions: ${{ env.extensions }}
                  coverage: xdebug
                  tools: composer
              env:
                  COMPOSER_TOKEN: ${{ secrets.GITHUB_TOKEN }}

            - name: Setup Laravel 13
              run: |
                  composer require "laravel/framework:13.*" "orchestra/testbench:11.*" --no-interaction --no-update
                  composer update --prefer-stable --prefer-dist --no-interaction --no-suggest

            - name: Run coverage
              run: |
                  vendor/bin/phpunit -c phpunit_gt_81.xml --coverage-clover=coverage.clover
